# A single-node default: the coordinator also schedules work on itself. The adhoc and batch
# clusters replace this file; a bare `docker run` and the smoke use it as is.
coordinator=true
node-scheduler.include-coordinator=true
http-server.http.port=8080
discovery.uri=http://localhost:8080
