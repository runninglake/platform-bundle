# One environment name for every node of one cluster; nodes with a different value do not
# join. The data directory is under /work, the only writable path in the pod.
node.environment=runninglake
node.data-dir=/work/data
